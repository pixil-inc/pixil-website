export interface Service {
  heading: string;
  description: string;
  icon: string;
}

export interface Client {
  name: string;
  industry?: string;
  address1: string;
  address2: string;
  phone: string;
  email: string;
}
